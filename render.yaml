services:
  - type: web
    name: smart-insole-analytics
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python professional_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
      - key: PORT
        value: 8000
